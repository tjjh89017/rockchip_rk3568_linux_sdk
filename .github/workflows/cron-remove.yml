---
name: Schedule clean up

on:
  schedule:
    # remove on Sunday 00:00 UTC
    - cron: '0 0 * * 0'

jobs:
  schedule_remove:
    runs-on: self-hosted
    steps:
      - name: Remove cache
        run: |
          rm -rf $GITHUB_WORKSPACE && mkdir $GITHUB_WORKSPACE
